Created rows and columns with Bootstrap
dynamically add data attributes with jQuery
install Moment.js package
call it in with Moment() 
take current date and display dynamically with jQuery
create a variable with that contains current hour in military time
compare that in conditionals that determine background color
take input value and store it in an array in local storage